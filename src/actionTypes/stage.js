import { stagePrefix } from '../constants/actions';

export default {
    RESET: stagePrefix + "RESET",
    LOAD: stagePrefix + "LOAD",
    SET_ROOT: stagePrefix + "SET_ROOT",
    ADD_SCENE: stagePrefix + "ADD_SCENE",
    REMOVE_SCENE: stagePrefix + "REMOVE_SCENE"
};